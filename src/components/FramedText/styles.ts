import styled from "styled-components";
import FramedText from "../FramedText";

export const FramedTextWithTarja1 = styled(FramedText).attrs({
  tarjaHeight: "1.75rem",
})``;

export const FramedTextWithTarja2 = styled(FramedText).attrs({
  tarjaHeight: "2.25rem",
})``;
